export const metadata = {
  title: 'About',
  description:
    'Discover the story behind Bellissimaa — a premium saree brand from Pune, curating timeless handcrafted sarees that blend heritage with modern elegance.',
};

export default function AboutLayout({ children }) {
  return children;
}
